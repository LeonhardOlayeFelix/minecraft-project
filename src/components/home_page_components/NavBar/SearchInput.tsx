import { Input } from "@chakra-ui/react";

const SearchInput = () => {
  return (
    <Input borderRadius={20} placeholder="Search Items..." variant={"filled"} />
  );
};

export default SearchInput;
